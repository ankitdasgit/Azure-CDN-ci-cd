name: deploy_static_website

on:
    push:
        branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: azure/login@v1
      with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Upload to blob storage
      uses: azure/CLI@v1
      with:
        inlineScript: |
            az storage blob upload-batch --account-name storageforcicdobject --account-key ${{ secrets.AZURE_STORAGE_KEY }} --auth-mode key --overwrite -d '$web' -s .
    - name: Purge CDN endpoint
      uses: azure/CLI@v1
      with:
        inlineScript: |
           az cdn endpoint purge --content-paths  "/*" --profile-name "CDN-profile1forcicd" --name "staticsitevc" --resource-group "ankit-resource-group"
  # Azure logout
    - name: logout
      run: |
            az logout
      if: always()







# name: Deploy to Azure CDN

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
        
#       - name: Install Azure CLI
#         run: |
#           curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
          
#       - name: Azure login
#         run: az login --service-principal -u ${{ secrets.AZURE_CLIENT_ID }} -p ${{ secrets.AZURE_CLIENT_SECRET }} --tenant ${{ secrets.AZURE_TENANT_ID }}

#       - name: Set subscription
#         run: az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}

#       - name: Upload website files to Azure Storage Account
#         run: az storage blob upload-batch --account-name storageforcicdobject --destination \$web --source C:/Users/DELL/OneDrive/Desktop/VirtueCloud/Azure-task/index.html --no-progress

#       - name: Configure CDN endpoint
#         run: |
#           az cdn endpoint create --name Cdn-endpoint-name1cicd --profile-name CDN-profile1forcicd --origin storageforcicdobject.z13.web.core.windows.net --origin-host-header storageforcicdobject.z13.web.core.windows.net --resource-group ankit-resource-group --cdn-type Standard_Verizon
      

   
